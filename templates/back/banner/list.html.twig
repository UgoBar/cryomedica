{% extends 'back_base.html.twig' %}

{% block body %}
    <div class="row ms-0">
        <a class="btn bg-gradient-primary mb-1" type="submit" style="width: fit-content" href="{{ path('create_banner') }}">Ajouter une bannière</a>
    </div>
{% endblock %}
